const body = document.querySelector("body") ;

function createSticky(){
    let writingPad = createBox() ;
    const textarea = document.createElement("textarea") ;
    writingPad.appendChild(textarea) ;
}
